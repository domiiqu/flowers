// print.js — the study's renderer now lives in the app proper. This file
// stays a re-export so the study keeps exercising the exact same engine
// the day page uses.
export * from '../src/print.js';
